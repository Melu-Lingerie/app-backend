spring:
  application:
    name: melu-lingerie
  profiles:
    active: dev
  mail:
    host: smtp.gmail.com
    port: 587
    username: leikoplasterr@gmail.com
    password: 'uotlpkeohedwjtdc'
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true



  # Data Source Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: navvrks9
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: false
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  # JPA & Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 16
        jdbc:
          batch_size: 20
          order_inserts: true
          order_updates: true
        connection:
          provider_disables_autocommit: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server:
  port: 8080
  shutdown: graceful

# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true

# Logging Configuration
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.yourcompany.yourapp: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
##TODO какой задать секрет  и в каких единицах здесь установлено значение времени?
jwt:
  secret: ${JWT_SECRET:change_me_to_very_long_256bit_secret_2025}
  # Время жизни токенов в секундах
  access-token-ttl-seconds: 900      # 15 минут
  refresh-token-ttl-seconds: 2592000 # 30 дней

app:
  verification:
    code-ttl-minutes: 15
    max-attempts: 3
    resend-cooldown-minutes: 1
    ##TODO ссделать 72 часа
    cleanup-interval-ms: 3600000  # 1 час в миллисекундах

